% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{scale_color_biopalette_gradient}
\alias{scale_color_biopalette_gradient}
\alias{scale_colour_biopalette_gradient}
\alias{scale_fill_biopalette_gradient}
\title{Continuous ggplot2 Gradients from a biopalette Palette}
\usage{
scale_color_biopalette_gradient(
  palette,
  ...,
  type = NULL,
  reverse = FALSE,
  values = NULL,
  midpoint = NULL,
  transform = "identity",
  palettes_dir = NULL
)

scale_colour_biopalette_gradient(
  palette,
  ...,
  type = NULL,
  reverse = FALSE,
  values = NULL,
  midpoint = NULL,
  transform = "identity",
  palettes_dir = NULL
)

scale_fill_biopalette_gradient(
  palette,
  ...,
  type = NULL,
  reverse = FALSE,
  values = NULL,
  midpoint = NULL,
  transform = "identity",
  palettes_dir = NULL
)
}
\arguments{
\item{palette}{Character. Name of the palette.}

\item{...}{Passed to \code{\link[ggplot2:scale_gradient]{ggplot2::scale_colour_gradientn()}} or
\code{\link[ggplot2:scale_gradient]{ggplot2::scale_fill_gradientn()}}, for example \code{name}, \code{breaks}, \code{labels},
\code{limits}, \code{transform}, \code{na.value}, and \code{guide}.}

\item{type}{Character. \code{"sequential"} or \code{"diverging"}. If \code{NULL}, the
type is detected automatically.}

\item{reverse}{Logical. Reverse the palette. When \code{values} is supplied, its
spacing is reversed with the colours. Default: \code{FALSE}.}

\item{values}{Optional numeric positions for the palette colours, as accepted
by \code{\link[ggplot2:scale_gradient]{ggplot2::scale_colour_gradientn()}}. Must have one value per palette
colour, be non-decreasing, and lie in \verb{[0, 1]}.}

\item{midpoint}{Optional finite numeric value to place at the visual centre
of a diverging palette. Values are rescaled with equal units on either side
of this point, so the shorter side of an asymmetric data range does not
reach the palette's extreme colour. \code{midpoint} cannot be combined with
\code{values} or a custom \code{rescaler}. The default, \code{NULL}, applies ordinary
range-based scaling.}

\item{transform}{A transformation specification accepted by
\code{\link[ggplot2:continuous_scale]{ggplot2::continuous_scale()}}. The same transformation is applied to
\code{midpoint} before the gradient is rescaled, so the centre remains correct
under transformations such as \code{"log10"}. Default: \code{"identity"}.}

\item{palettes_dir}{Character. Directory holding a palette collection. If
\code{NULL}, the palettes bundled with the package are used.}
}
\value{
A ggplot2 continuous scale.
}
\description{
Build a continuous colour or fill gradient from a sequential or diverging
biopalette palette. The complete palette supplies the gradient stops to
\code{\link[ggplot2:scale_gradient]{ggplot2::scale_colour_gradientn()}} or \code{\link[ggplot2:scale_gradient]{ggplot2::scale_fill_gradientn()}}.
Gradients are interpolated in Lab colour space, the same space used when
\code{\link[=get_palette]{get_palette()}} resamples sequential and diverging palettes.
}
\details{
Qualitative palettes are rejected because interpolating unordered category
colours does not produce a meaningful continuous scale. Use
\code{\link[=scale_color_biopalette]{scale_color_biopalette()}} or \code{\link[=scale_fill_biopalette]{scale_fill_biopalette()}} for those palettes.
}
\examples{
library(ggplot2)

ggplot(mtcars, aes(wt, mpg, colour = hp)) +
  geom_point(size = 3) +
  scale_color_biopalette_gradient("mitonuclear_blue")

ggplot(mtcars, aes(factor(cyl), factor(gear), fill = mpg - mean(mpg))) +
  geom_tile() +
  scale_fill_biopalette_gradient("walter_white", midpoint = 0)

}

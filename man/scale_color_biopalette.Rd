% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{scale_color_biopalette}
\alias{scale_color_biopalette}
\alias{scale_colour_biopalette}
\alias{scale_fill_biopalette}
\title{Discrete ggplot2 Scales from a biopalette Palette}
\usage{
scale_color_biopalette(
  palette,
  ...,
  type = NULL,
  reverse = FALSE,
  palettes_dir = NULL
)

scale_colour_biopalette(
  palette,
  ...,
  type = NULL,
  reverse = FALSE,
  palettes_dir = NULL
)

scale_fill_biopalette(
  palette,
  ...,
  type = NULL,
  reverse = FALSE,
  palettes_dir = NULL
)
}
\arguments{
\item{palette}{Character. Name of the palette.}

\item{...}{Passed to \code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}}, for example \code{name},
\code{breaks}, \code{labels}, \code{limits}, \code{na.value}, \code{drop}, and \code{guide}.}

\item{type}{Character. One of \code{"sequential"}, \code{"diverging"}, or
\code{"qualitative"}. If \code{NULL}, the type is detected automatically.}

\item{reverse}{Logical. Reverse the palette. Default: \code{FALSE}.}

\item{palettes_dir}{Character. Directory holding a palette collection. If
\code{NULL}, the palettes bundled with the package are used.}
}
\value{
A ggplot2 discrete scale.
}
\description{
Use a named biopalette palette for a discrete colour or fill mapping. The
scale asks \code{\link[=get_palette]{get_palette()}} for exactly as many colours as the trained data
has levels. Qualitative palettes therefore use their first \code{n} colours,
while sequential and diverging palettes sample \code{n} colours across their
complete ramps.
}
\details{
All three palette types are supported. Qualitative palettes suit unordered
categories; sequential and diverging palettes can be useful for ordered
categories.
}
\examples{
library(ggplot2)

ggplot(iris, aes(Sepal.Length, Sepal.Width, colour = Species)) +
  geom_point() +
  scale_color_biopalette("three_body")

ggplot(iris, aes(Species, Sepal.Length, fill = Species)) +
  geom_boxplot() +
  scale_fill_biopalette("three_body", guide = "none")

}
